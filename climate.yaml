  - platform: generic_thermostat
    name: LivingRoom
    #heater: input_boolean.thermostat_switch
    heater: switch.thermostat_switch
    target_sensor: sensor.netatmo_indoor_temperature
    min_temp: 18
    max_temp: 30
    #target_temp: '{{ states.input_number.box_target_temp.state | float }}'
    target_temp: 24
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 60
