  - platform: template
    switches:
      thermostat_switch:
        value_template: "{{ is_state('sensor.thermostat_switch', 'on') }}"
        turn_on:
          service: switch.turn_on
          data:
            entity_id: switch.thermostat_switch
        turn_off:
          service: switch.turn_off
          data:
            entity_id: switch.thermostat_switch
